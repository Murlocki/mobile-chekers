<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingEnd="16dp"
    android:paddingStart="0dp"
    android:paddingVertical="15dp"
    android:background="@color/default_background_color"
    >
        <androidx.cardview.widget.CardView
            android:layout_height="wrap_content"
            android:layout_width="wrap_content"
            app:cardCornerRadius="@dimen/nav_layout_back_button_corner_radius"

            android:layout_gravity="end"
            >
                <Button
                    android:id="@+id/backButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/back_button_text"
                    android:textSize="@dimen/nav_layout_back_button_text_size"
                    android:padding="@dimen/nav_layout_back_button_paddings"
                    android:background="@color/default_text_color"
                    android:textColor="@color/default_background_color"/>
        </androidx.cardview.widget.CardView>

</LinearLayout>
